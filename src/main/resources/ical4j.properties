# Intentionally empty. Present solely so ical4j's Configurator finds a config file on the
# classpath at startup and does NOT log "ical4j.properties not found." at INFO. That log call
# happened during SLF4J's initialization window, which also triggered an "SLF4J(W): ... calls
# during the initialization phase ... replayed" warning — both vanish once the file is found.
# No overrides are needed; all ical4j defaults apply.
